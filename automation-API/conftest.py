import pytest

@pytest.fixture
def clean_resources_from_db():
    # import yaml
    # with open("common.yaml", 'r') as stream:
    #     data_loaded = yaml.safe_load(stream)
    # web_url = data_loaded['variables']['toolkit-qa']['host']
    # import requests
    # resp = requests.get("{0}/projects".format(web_url))
    # for project in resp.json()['projects']:
    #     requests.delete("{0}/projects/{1}".format(web_url, project['uuid']))
    # resp = requests.get("{0}/clients".format(web_url))
    # for client in resp.json()['clients']:
    #     requests.delete("{0}/clients/{1}".format(web_url, client['uuid']))
    pass