version: 0.2

env:
  parameter-store:
    username: CICD_JFROG_USERNAME
    password: CICD_JFROG_PASSWORD

phases:
  pre_build:
    commands:
      - $(aws ecr get-login --region $AWS_DEFAULT_REGION --no-include-email)
      - ECR_URI=946454339744.dkr.ecr.us-east-1.amazonaws.com/toolkit-automation:latest
      - export username=username
      - export password=password
      - echo $username
      - echo $password
      - export PIP_INDEX_URL=https://$username:$password@prodjfrogapplicationus.jfrog.io/prodjfrogapplicationus/api/pypi/pypi/simple
      - echo $PIP_INDEX_URL
  build:
    commands:
      - docker build -t $ECR_URI .
  post_build:
    commands:
      - docker push $ECR_URI
